{
  "name": "slur-client",
  "version": "1.0.0",
  "description": "Messaging client (for learning purposes)",
  "scripts": {
    "start": "lite-server -c bs-config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "nodemon": "^2.0.20"
  }
}
